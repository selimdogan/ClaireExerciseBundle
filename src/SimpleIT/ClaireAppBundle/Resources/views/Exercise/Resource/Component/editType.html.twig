<h2>Type de ressource</h2>
{% if locked %}<div>Édition vérouillée</div>{% endif %}

<form action="{{ path('simple_it_claire_component_exercise_resource_type_edit',
{ 'resourceId': resource.id}) }}" method="post" {{ form_enctype(form) }}>
    {{ form_errors(form) }}
    {{ form_errors(form.type) }}
    {{ form_widget(form.type) }}
    {{ form_rest(form) }}
    <input type="submit">
    {#Remove submit#}
</form>
