<form action="{{ path('simple_it_claire_component_course_course_edit', { 'courseId': course.id}) }}" method="post" {{ form_enctype(form) }}>
    {{ form_errors(form) }}
    {{ form_errors(form.displayLevel) }}
    {{ form_widget(form.displayLevel) }}
    {{ form_rest(form) }}
    <input type="submit">
    {#Remove submit#}
</form>
