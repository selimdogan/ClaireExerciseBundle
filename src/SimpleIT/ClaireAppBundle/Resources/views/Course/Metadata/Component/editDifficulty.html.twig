<span class="align-middle background-icon-black">
    <i class="icon-level"></i>
</span>

<form class="js-course-difficulty" action="{{ path('simple_it_claire_component_course_course_metadata_difficulty_edit', { 'courseId': courseId}) }}" method="post" {{ form_enctype(form) }}>
    {{ form_errors(form) }}
    {{ form_errors(form.value) }}
    {{ form_widget(form.value) }}
    {{ form_rest(form) }}
    <input type="submit">
    {#Remove submit#}
</form>
